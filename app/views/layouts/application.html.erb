<!DOCTYPE html>
<html>

<head>
  <title>Seatyourself</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/css?family=Lato:400,100,300,300italic" rel="stylesheet" type="text/css">
  <link href="css/bootstrap.min.css" rel="stylesheet">
</head>

<header>
  <%= link_to image_tag("icon.png"), root_path %>
  <nav>
    <ul>
      <% if current_user %>
        <li>Hello <%= current_user.name %></li>
        <li><%= link_to 'Log Out', sessions_path, method: :delete %></li>
        <li><%= link_to 'My Reservation', user_path(current_user)%></li>
      <% else %>
        <li><%= link_to 'Log In', new_sessions_path %></li>
        <li><%= link_to 'Sign Up', new_user_path %></li>
      <% end %>
    </ul>
  </nav>
</header>

<main>
  <body>
    <% if flash.any? %>
       <% flash.each do |key, value| %>
         <p class='<%= key %>'>
         <%= value %>
         </p>
       <% end %>
     <% end %>

  <%= yield %>
  <footer>
    Made with Love by Theresa Shen
  </footer>

  </body>
</main>

</html>
